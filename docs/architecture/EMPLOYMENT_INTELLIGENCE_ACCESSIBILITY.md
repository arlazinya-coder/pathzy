# Employment Intelligence Accessibility

Phase 3G uses:

- semantic headings;
- keyboard-accessible buttons and links;
- `aria-live` for update/error state;
- `aria-expanded` for recommendation explanations;
- shared progress semantics through `ProgressBar`;
- text labels in addition to colour;
- visible focus styles.

Long French labels and high-guidance wording must remain readable on mobile.
