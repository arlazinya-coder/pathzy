# Employment Intelligence Privacy

Persisted records minimise sensitive data.

The input snapshot hash stores a deterministic reference to relevant versions and summary codes. It does not persist full raw diagnosis answers or full Professional Identity content.

Detailed derived payloads may include barriers, missing information and explanations, but API summaries return only safe summary fields needed for Home and future consumers.

Logs contain safe event metadata only.
